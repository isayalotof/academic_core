-- Увеличить размер поля name в таблице groups
-- для поддержки длинных имен групп (несколько групп в одной строке Excel)

-- Увеличить name до 255 символов
ALTER TABLE groups 
    ALTER COLUMN name TYPE VARCHAR(255);

-- Обрезать short_name до 50 символов (если нужно)
-- short_name остается VARCHAR(50) - это короткое имя

COMMENT ON COLUMN groups.name IS 'Полное название группы (до 255 символов, может содержать несколько групп через разделители)';

